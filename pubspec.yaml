name: veterinaria_movil
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Iconos de estilo iOS
  cupertino_icons: ^1.0.8

  # Manejo de estados y navegación
  get: ^4.7.2

  # Firebase
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2

  # Formato de fechas
  intl: ^0.18.0

  # Geolocalización y direcciones
  geolocator: ^13.0.1
  geocoding: ^3.0.0

  # 🔹 Abrir enlaces externos (como Google Maps)
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    # Include the whole data folder so the JSON asset is recognized by Flutter tools
    - assets/data/
    # keep explicit file reference for clarity
    - assets/data/colombia_locations.json

